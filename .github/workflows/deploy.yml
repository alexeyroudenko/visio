name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-visio
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install
        run: npm ci

      - name: Build
        env:
          # Public project key — baked into the bundle by design. A repo
          # *variable*, not a secret: masking it in logs buys nothing when it
          # ships to every visitor, and an empty value simply disables analytics.
          VITE_POSTHOG_KEY: ${{ vars.VITE_POSTHOG_KEY }}
          VITE_POSTHOG_HOST: ${{ vars.VITE_POSTHOG_HOST }}
        run: npm run build

      - name: Setup SSH
        env:
          AA_SSH_KEY: ${{ secrets.AA_SSH_KEY }}
          AA_SSH_HOST: ${{ secrets.AA_SSH_HOST }}
          AA_SSH_USER: ${{ secrets.AA_SSH_USER }}
        run: |
          mkdir -p ~/.ssh
          # Secret is base64(private key) — avoids multiline/CRLF corruption from Windows uploads
          printenv AA_SSH_KEY | tr -d ' \n\r\t' | base64 -d > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keygen -y -f ~/.ssh/deploy_key >/dev/null
          ssh-keyscan -H "$AA_SSH_HOST" >> ~/.ssh/known_hosts
          ssh -i ~/.ssh/deploy_key -o BatchMode=yes -o IdentitiesOnly=yes -o ConnectTimeout=10 \
            "${AA_SSH_USER}@${AA_SSH_HOST}" 'echo ssh-ok'

      - name: Sync to /var/www/AA/visio
        env:
          AA_SSH_HOST: ${{ secrets.AA_SSH_HOST }}
          AA_SSH_USER: ${{ secrets.AA_SSH_USER }}
        run: |
          REMOTE="${AA_SSH_USER}@${AA_SSH_HOST}"
          REMOTE_DIR="/var/www/AA/visio"
          rsync -avz --delete --chmod=D755,F644 \
            -e "ssh -i $HOME/.ssh/deploy_key -o BatchMode=yes" \
            dist/ "${REMOTE}:${REMOTE_DIR}/"
          ssh -i "$HOME/.ssh/deploy_key" -o BatchMode=yes "$REMOTE" \
            "chmod -R a+rX '${REMOTE_DIR}'; if [[ -x /var/www/AA/generate-index.sh ]]; then bash /var/www/AA/generate-index.sh; fi"

      - name: Verify live URL
        run: |
          code="$(curl -s -o /dev/null -w '%{http_code}' -L 'https://visio.aa.arthew0.online/')"
          echo "HTTP ${code}"
          case "$code" in
            200|301|302) ;;
            *) echo "Unexpected status: ${code}" >&2; exit 1 ;;
          esac
